class ResUtility {
    public static getMapRes(fileList: string[], mapID: number): void
    {
        /*
        var city_cnf = ConfigMgr.getInstance().getConfig("city")[mapID];
        var map_cnf = ConfigMgr.getInstance().getMapConfig(mapID);

        var map_res = map_cnf.map_res
        var offset = map_cnf.tile_size
        var tile_w = Math.floor((city_cnf.width - 1) / offset + 1)
        var tile_h = Math.floor((city_cnf.height - 1) / offset + 1)
        var image_file = "map_" + map_res + "_";

        for(var i = 1; i <= tile_h; i++)
        {
            for(var j = 1; j <= tile_w; j++)
            {
                fileList.push(image_file + i + "X" + j + "_jpg");
            }
        }

        fileList.push(image_file + "mini_map_jpg");
        */
    }
}