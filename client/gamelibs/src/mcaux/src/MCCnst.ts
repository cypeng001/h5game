namespace h5game
{

export enum MCST {
    UNINIT       = 1,
    LOAD         = 2,
    UNLOAD       = 3,
};

export enum MCPST {
    UNINIT      = 1,
    UNLOAD      = 2,
    LOADING     = 3,
    LOADED      = 4,
};

}